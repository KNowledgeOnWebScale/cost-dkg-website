{
  "version": "1.1.1",
  "watch": {
    "run": "config.yaml"
  },
  "scripts": {
    "start": "npx walder -c config.yaml --no-cache",
    "watch": "npm-watch"
  },
  "dependencies": {
    "date-fns": "^2.16.1",
    "date-fns-tz": "^1.1.6",
    "markdown-it": "^12.3.2",
    "walder": "^2.2.2"
  },
  "devDependencies": {
    "npm-watch": "^0.7.0"
  }
}
